{% extends 'base.html.twig' %}

{% block title %}Imprevisto{% endblock %}

{% block body %}
    <h1>Imprevisto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imprevisto.id }}</td>
            </tr>
            <tr>
                <th>Imprevisto</th>
                <td>{{ imprevisto.imprevisto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('imprevisto_index') }}">back to list</a>

    <a href="{{ path('imprevisto_edit', {'id': imprevisto.id}) }}">edit</a>

    {{ include('imprevisto/_delete_form.html.twig') }}
    
{% endblock %}
