{% extends 'base.html.twig' %}

{% block title %}Viaje{% endblock %}

{% block body %}
    <h1>Viaje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viaje.id }}</td>
            </tr>
            <tr>
                <th>Salida</th>
                <td>{{ viaje.salida ? viaje.salida|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Llegada</th>
                <td>{{ viaje.llegada ? viaje.llegada|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('viaje_index') }}">volver a la lista</a>

    <a href="{{ path('viaje_edit', {'id': viaje.id}) }}">Editar</a>

    {{ include('viaje/_delete_form.html.twig') }}
{% endblock %}
