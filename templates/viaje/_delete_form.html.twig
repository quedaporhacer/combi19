<form method="post" action="{{ path('viaje_delete', {'id': viaje.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ viaje.id) }}">
    <button class="btn">Delete</button>
</form>
