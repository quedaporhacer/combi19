{% extends 'base.html.twig' %}

{% block title %}Lugar{% endblock %}

{% block body %}
    <h1>Lugar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lugar.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ lugar.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lugar_index') }}">back to list</a>

    <a href="{{ path('lugar_edit', {'id': lugar.id}) }}">edit</a>

    {{ include('lugar/_delete_form.html.twig') }}
{% endblock %}
