{% extends 'base.html.twig' %}

{% block title %}Nuevo Lugar{% endblock %}

{% block body %}
{{parent()}}
    <div class='container'>
        <br>
        <br>
        <br>
        <div class="shadow-lg p-3 mb-5 bg-body rounded">
        <h1>Crear nuevo Lugar</h1>

        {% for message in app.flashes('failed') %}

                <div class="alert alert-danger">
                    {{ message }}
                </div>
            {% endfor %}

        {{ include('lugar/_form.html.twig') }}

        <a href="{{ path('lugar_index') }}">volver al listado</a>
        </div>
    </div>
{% endblock %}
