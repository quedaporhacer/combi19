{% extends 'base.html.twig' %}

{% block title %}Lugar index{% endblock %}

{% block body %}
    <h1>Lugar index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lugar in lugars %}
            <tr>
                <td>{{ lugar.id }}</td>
                <td>{{ lugar.nombre }}</td>
                <td>
                     {{ include('lugar/_delete_form.html.twig') }}
                    <a href="{{ path('lugar_edit', {'id': lugar.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no hya lugares</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lugar_new') }}">Crear Lugar</a>
{% endblock %}
