{% extends 'base.html.twig' %}

{% block title %}Editar Perfil{% endblock %}

{% block body %}
    <div class="container">
        <br>
        <br>
        <br>
        <div class="shadow-lg p-3 mb-5 bg-body rounded">
        <h1>Editar Perfil</h1>

        {{ include('pasajero/_form.html.twig', {'button_label': 'Guardar'}) }}

        <a href="{{ path('pasajero_show', {'id': pasajero.id})  }}">volver a la lista</a>


        {#{{ include('pasajero/_delete_form.html.twig') }}#}
        </div>
    </div>
{% endblock %}
