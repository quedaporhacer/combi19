{% extends 'base.html.twig' %}

{% block title %}Administrador{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hola Administrador</h1>

    Ingresar a la informacion de:
        <a class="" href="{{ path('chofer_index') }}">Choferes</a>
        <a class="" href="{{ path('combi_index') }}">Combi</a>
        <a class="" href="{{ path('viaje_index') }}">Viaje</a>
        <a class="" href="{{ path('insumo_index') }}">Insumo</a>
        <a class="" href="{{ path('lugar_index') }}">Lugar</a>
        <a class="" href="{{ path('ruta_index') }}">Ruta</a>
</div>
{% endblock %}
