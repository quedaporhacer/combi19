<form style="display: inline;" method="post" action="{{ path('combi_delete', {'id': combi.id}) }}" onsubmit="return confirm('Estas seguro que desea borrar esta combi?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ combi.id) }}">
    <button class="btn btn-danger">Borrar</button>
</form>
