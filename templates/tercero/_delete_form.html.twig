<form method="post" action="{{ path('tercero_delete', {'id': tercero.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tercero.id) }}">
    <button class="btn">Delete</button>
</form>
