{% extends 'base.html.twig' %}

{% block title %}New Tercero{% endblock %}

{% block body %}
    <br><br><br>
    <div class="container ">
        <div class="shadow-lg p-3 mb-5 bg-body rounded">
              {% for message in app.flashes('failed') %}

                    <div class="alert alert-danger">
                        {{ message }}
                    </div>
                    
            {% endfor %}
        
            <h1>¿Desea comprar un pasaje para un tercero?</h1>

            {{ include('tercero/_form.html.twig') }}

            <a class="btn btn-primary" href="{{ path('tercero_index') }}">Agregar Insumos</a>
            <a class="btn btn-success" href="{{ path('compra_finish',{'id': ticket.id}) }}">Finalizar Compra</a>
            <a class="btn btn-danger" href="{{ path('tercero_index') }}">Cancelar</a>
        </div>
    </div>      
{% endblock %}
